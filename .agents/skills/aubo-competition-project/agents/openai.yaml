interface:
  display_name: "AUBO 国赛工程约束"
  short_description: "维护既有架构、接口契约、标定链路、现场调试与权限边界"
  default_prompt: "Use $aubo-competition-project to review this requested change against the established project architecture and interface contracts before editing."
policy:
  allow_implicit_invocation: true
